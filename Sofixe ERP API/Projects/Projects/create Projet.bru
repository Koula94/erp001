meta {
  name: create Projet
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/projects/projects/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
