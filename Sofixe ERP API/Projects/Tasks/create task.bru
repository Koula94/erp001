meta {
  name: create task
  type: http
  seq: 1
}

post {
  url: {{base_url}}/api/projects/tasks/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
