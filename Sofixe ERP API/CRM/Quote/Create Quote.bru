meta {
  name: Create Quote
  type: http
  seq: 2
}

post {
  url: {{base_url}}/api/crm/quotes/
  body: json
  auth: inherit
}

headers {
  Authorization: Bearer {{access_token}}
}

settings {
  encodeUrl: true
}
